backend = "glx";
vsync = true;
glx-use-copysubbuffer-mesa = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;

shadow = false
fading = false
inactive-opacity = 0.55;

opacity-rule = [
	"100:name *= 'rofi'",
	"100:name *= 'i3lock'"
]
